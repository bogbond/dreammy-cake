/* v3.26 Formly minimal mode.
   No dynamic metadata, redirect, AJAX, progress or honeypot fields are added.
   Forms submit natively to https://formly.email/submit with access_key only. */
(function(){ 'use strict'; })();
